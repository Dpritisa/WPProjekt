{
  "dependencies": {
    "express": "^4.19.2",
    "firebase": "^10.13.1",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "heroku-postbuild": "cd ./dino-app && npm install && npm install -g @angular/cli && npm run build"
    
  }
}
